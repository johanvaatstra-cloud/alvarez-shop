{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm install && pnpm --filter @alvarez/backend... build"
  },
  "deploy": {
    "startCommand": "pnpm --filter @alvarez/backend start"
  }
}
